include ./uikit-stages-track/index.pug

mixin addUIKitStage(id, stages, stage, orientation, invert)
	div.uikit.uikit-stages(id=id, stages=stages, stage=stage, type=orientation, invert=invert)
		+addUIKitStages_Track()